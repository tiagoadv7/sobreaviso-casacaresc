/**
 * @deprecated Este arquivo foi substituído pelo Firebase Auth + Firestore.
 * Os usuários agora são gerenciados no painel admin (Firebase Authentication).
 * Este arquivo pode ser removido com segurança.
 */
export {};
